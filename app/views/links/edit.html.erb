<h4 class="center">Edit Link</h4>
<div class="container">
  <%= form_for @link, url: link_path(@link) do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.label :url %>
    <%= f.text_field :url %>
    <%= f.submit "Update Link", class: "btn" %>
   <% end %>
</div>
